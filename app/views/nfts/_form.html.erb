<%= form_with(model: nft) do |form| %>
  <% if nft.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(nft.errors.count, "error") %> prohibited this nft from being saved:</h2>

      <ul>
        <% nft.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :token_link, style: "display: block" %>
    <%= form.text_field :token_link %>
  </div>

  <div>
    <%= form.label :owner_name, style: "display: block" %>
    <%= form.text_field :owner_name %>
  </div>

  <div>
    <%= form.label :wallet_address, style: "display: block" %>
    <%= form.text_field :wallet_address %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
